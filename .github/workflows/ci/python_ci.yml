on:
  workflow_call:
    inputs:
      python-version:
        required: false
        type: string
        default: "3.x"
      test-results-artifact-name:
        required: false
        type: string
        default: "test-results"

jobs:
  linting:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ inputs.python-version }}
      - name: linting
        id: linting
        run: |
          echo "todo"
    tests:
      uses: nicburgt/.github/.github/workflows/ci/tests.yml@c8c0a1b49a4b442faf22c8465f955e8dcc126fb0
      with:
        python-version: ${{ inputs.python-version }}
        test-results-artifact-name: ${{ inputs.test-results-artifact-name }}